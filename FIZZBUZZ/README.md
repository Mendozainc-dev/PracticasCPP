# App de Fizzbuzz

Esta app la mire una vez que andaba buscando practicas pequenas por que era la primera vez que estana tocando un poco mas a fondo el lenguaje cpp y pues en si esa fue la logica 
final que pude implementar 

# Como ejecutar

Para ejecutar la app solo usar el siguiente comando 

```bash
./Fizzbuzz
```

Y ya con eso compila y muestra en consola 

nota: tomar en cuenta estar en la ruta para que funcione la app
